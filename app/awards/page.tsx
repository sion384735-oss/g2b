import SiteHeader from "../components/SiteHeader";
import { bids, won } from "../data";

export default function Awards(){return <div className="page-shell"><SiteHeader active="awards"/><div className="page-wrap"><div className="page-title"><div className="crumb">홈 〉 낙찰정보</div><h1>낙찰정보</h1><p>최근 개찰된 공고의 낙찰 결과와 투찰률을 확인하세요.</p></div><div className="demo-notice">ⓘ 낙찰 결과 예시 데이터입니다. 계산된 수치는 ‘계산값’으로 구분합니다.</div><section className="panel filters"><div className="filter-grid"><div className="field search-wide"><label>낙찰결과 검색</label><input placeholder="공고명, 낙찰업체, 발주기관"/></div><div className="field"><label>업무구분</label><select><option>전체</option><option>공사</option><option>용역</option><option>물품</option></select></div><div className="field"><label>개찰일</label><select><option>최근 1개월</option><option>최근 3개월</option></select></div><div className="field"><label>지역</label><select><option>전체 지역</option><option>서울</option><option>경기</option></select></div></div></section><div className="result-bar"><strong>최근 낙찰 <em>{bids.length}</em>건</strong></div><section className="panel data-table"><div className="data-head" style={{gridTemplateColumns:"54px 2fr 1fr 120px 110px 110px 42px"}}><span>순위</span><span>공고명</span><span>낙찰업체</span><span>낙찰금액</span><span>투찰률</span><span>개찰일</span><span></span></div>{bids.map((b,i)=><article className="data-row" style={{gridTemplateColumns:"54px 2fr 1fr 120px 110px 110px 42px"}} key={b.id}><span className="award-rank">{i+1}</span><a className="bid-title" href={`/bids/${b.id}`}><strong>{b.title}</strong><small>{b.agency}</small></a><span>{["주식회사 테크원","한빛건설(주)","에이아이솔루션","미래정책연구원"][i%4]}</span><span className="money">{won(Math.round(b.basePrice*.8817))}</span><span>88.17% <small>(계산값)</small></span><span>2026-08-{23-i}</span><a href={`/bids/${b.id}`}>→</a></article>)}</section></div></div>}
